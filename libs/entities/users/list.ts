import { Account, Address, AdminProfile, BusinessProfile, Meta, RefreshToken, Role, Status, Store, UserProfile } from ".";

export const userEntities = [
    Account,
    Address,
    AdminProfile,
    BusinessProfile,
    Meta,
    RefreshToken,
    Role,
    Status,
    Store,
    UserProfile
]